// 播放模式
export const playMode = {
  sequence: 0, // 顺序播放
  loop: 1, // 单曲循环
  random: 2 // 随机播放
};

// 歌单获取顺序
export const SORT_ID = {
  new: 2, // 最新
  hot: 3, // 最热
  ratete: 4, // 评分
  default: 1 // 默认
};

// 歌单类型ID（默认为全部类型歌单）
export const DEFAULT_CATEGORY_ID = 10000000;
